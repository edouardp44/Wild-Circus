{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <ul class="nav justify-content-center nav-tabs">
            {% for category in categorys %}
                {% if category.spectacles is not empty %}
                    <li class="nav-item">
                        <a class="nav-link active"
                           href="{{ path('spectacle_category', {'id': category.id} ) }}">{{ category.name }}</a>
                    </li>
                {% endif %}
            {% endfor %}
        </ul>
        <h1 class="mt-5"> Tous nos animaux</h1>
        {% for categorys in spectacles %}
            {% for spectacle in categorys.spectacles %}
                <div class="row mt-5">
                    <div class="col-md-12">
                        <div class="media">
                            <img src="{{ spectacle.image }}" class="mr-3" alt="...">
                            <div class="media-body">
                                <h5 class="mt-0">{{ spectacle.name }}</h5>
                                {{ spectacle.description }}
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
{% endblock %}

            {% block title %}

            {% endblock %}
