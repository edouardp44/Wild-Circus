{% extends 'base.html.twig' %}

{% block title %}Edit Animals{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier</h1>
        {{ include('animals/_form.html.twig', {'button_label': 'Modifier'}) }}
        <a href="{{ path('admin_animals') }}">Retour à la liste</a>
        {{ include('animals/_delete_form.html.twig') }}
    </div>
{% endblock %}
