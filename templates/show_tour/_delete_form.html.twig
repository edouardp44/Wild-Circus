<form method="post" action="{{ path('show_tour_delete', {'id': show_tour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ show_tour.id) }}">
    <button class="btn">Delete</button>
</form>
