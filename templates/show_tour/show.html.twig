{% extends 'base.html.twig' %}

{% block title %}ShowTour{% endblock %}

{% block body %}
    <h1>ShowTour</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ show_tour.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ show_tour.city }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ show_tour.location }}</td>
            </tr>
            <tr>
                <th>BeginAt</th>
                <td>{{ show_tour.beginAt ? show_tour.beginAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>EndAt</th>
                <td>{{ show_tour.endAt ? show_tour.endAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('show_tour_index') }}">back to list</a>

    <a href="{{ path('show_tour_edit', {'id': show_tour.id}) }}">edit</a>

    {{ include('show_tour/_delete_form.html.twig') }}
{% endblock %}
